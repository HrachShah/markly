{
  "name": "markly",
  "version": "0.1.0",
  "description": "A small markdown link checker and report generator. Walks a directory of .md files, extracts every link, fetches the targets, and prints a per-file health report.",
  "main": "src/markly.js",
  "type": "module",
  "bin": {
    "markly": "src/markly.js"
  },
  "scripts": {
    "start": "node src/markly.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "keywords": [
    "markdown",
    "links",
    "checker",
    "docs",
    "static-site"
  ]
}
